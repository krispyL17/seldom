import { PlaceholderPage } from '@components/ui/PlaceholderPage'

export function AssistantPage() {
  return (
    <PlaceholderPage
      title="AI Assistant"
      description="Conversational AI with persistent memory — ask about training, tasks, or goals."
    />
  )
}
